// @ts-nocheck
// import { UsersService } from './userService';

describe('userService', () => {
  it('fetches user history correctly', async () => {
    // const history = await UsersService.getBookingHistory();
    // expect(history).toBeDefined();
    expect(true).toBe(true);
  });
});
